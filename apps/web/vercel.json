{
  "buildCommand": "cd apps/web && npm install && npx prisma generate && npm run build",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "installCommand": "npm install --prefix apps/web"
}